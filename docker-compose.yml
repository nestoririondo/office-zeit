services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - /var/lib/office-zeit/data:/app/data
    environment:
      - DATABASE_URL=file:/app/data/prod.db
    restart: unless-stopped
